import React from "react";

export const WorkflowBuilder = ({ workflowId }) => {
  return React.createElement(
    "div",
    { style: { padding: "2rem", color: "#888", textAlign: "center" } },
    "Workflow Studio is not available in this build."
  );
};
